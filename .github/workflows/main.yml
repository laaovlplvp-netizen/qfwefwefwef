name: Build
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - run: cl /EHsc AntiDodge.cpp
    - uses: actions/upload-artifact@v4
      with:
        name: AntiDodge
        path: AntiDodge.exe

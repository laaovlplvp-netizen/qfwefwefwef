name: Build AntiDodge

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
      - name: Compile AntiDodge
        run: cl /EHsc /Fe: AntiDodge.exe AntiDodge.cpp
      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: AntiDodge
          path: AntiDodge.exe
